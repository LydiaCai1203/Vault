FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY agents/ /app/agents/

# Analyzer receives trades via stdin (Executor proxy), returns analysis JSON
ENTRYPOINT ["python", "-m", "agents.analyzer.agent"]
